apiVersion: v1
kind: Service
metadata:
  name: maas-v2-client-api
  labels:
    app: maas-v2-client-api
spec:
  type: ClusterIP
  selector:
    app: maas-v2-client-api
  ports:
    - name: http
      port: 8000
      targetPort: http
